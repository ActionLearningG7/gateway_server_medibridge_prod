spring:
  application:
    name: api-gateway
  profiles:
    active: prod

  # Redis Configuration
  data:
    redis:
      url: redis://default:GOwUPRpFGRVtZMSiEEWlTapCvefmpsFj@hopper.proxy.rlwy.net:11415

server:
  port: ${PORT:8080}

# JWT Configuration (must match User Service configuration)
jwt:
  secret: MediBridge2026!Pr0duct1on$ecretK3y#H0sp1talManag3m3ntSyst3m@Secur1ty
  expiration: 3600000  # 1 hour in milliseconds
  refresh-expiration: 86400000  # 24 hours in milliseconds

eureka:
  client:
    service-url:
      defaultZone: https://servermedibridgeprod-production.up.railway.app/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: false
    lease-renewal-interval-in-seconds: 30
    lease-expiration-duration-in-seconds: 90

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,gateway
  endpoint:
    health:
      show-details: always

